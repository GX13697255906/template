<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dms.cloud.biz301.server.mapper.UnitParamRegisterInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dms.cloud.biz301.client.model.entity.UnitParamRegisterInfo">
        <id column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="unit_code" property="unitCode" />
        <result column="unit_name" property="unitName" />
        <result column="power_plant_code" property="powerPlantCode" />
        <result column="power_plant_name" property="powerPlantName" />
        <result column="unit_type" property="unitType" />
        <result column="rated_output" property="ratedOutput" />
        <result column="minimum_loading" property="minimumLoading" />
        <result column="cold_starting_info_time" property="coldStartingInfoTime" />
        <result column="warm_starting_info_time" property="warmStartingInfoTime" />
        <result column="hot_starting_info_time" property="hotStartingInfoTime" />
        <result column="status" property="status" />
        <result column="is_manual_flag" property="isManualFlag"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        create_time,
        update_time,
        id, unit_code, unit_name, power_plant_code, power_plant_name, unit_type, rated_output, minimum_loading, cold_starting_info_time, warm_starting_info_time, hot_starting_info_time, status,is_manual_flag
    </sql>

    <!--自定义关联查询-->
    <!-- 表名 -->
    <sql id="t_name">biz_unit_param_register_info</sql>
    <!-- 别名 -->
    <sql id="t_alias">biz_unit_param_register_info as info</sql>
    <!--外键表关联定义 -->
    <sql id="join"></sql>

    <!-- 自定义查询单个实体 -->
    <select id="getEntityMap" resultType="com.dms.cloud.common.mybatis.EntityMap">
        SELECT * FROM
        <include refid="t_alias"/>
        <include refid="join"/>
        ${ew.customSqlSegment}
    </select>

    <!-- 自定义关联查询分页 -->
    <select id="pageList" resultType="com.dms.cloud.common.mybatis.EntityMap">
        select ${ew.select} FROM
        <include refid="t_alias"/>
        <include refid="join"/>
        ${ew.customSqlSegment}
    </select>
</mapper>
